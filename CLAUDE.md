# CLAUDE.md

中文回答问题

## 重要文档

- **系统状态**: [SYSTEM_STATUS.md](SYSTEM_STATUS.md) - 实时系统状态，AI 查看此文档了解当前实现情况
- **项目指导**: 本文档 - 项目架构、开发规范、技术栈

---

本文件为 Claude Code (claude.ai/code) 在此代码库中工作时提供指导。

## 项目概述

Nano Banana 是一个全栈个人博客系统，前端使用 Vue 3，后端使用 Django REST Framework。博客支持技术文章 (blog)、项目展示 (projects)、生活日记 (life) 和快速笔记 (notes)。

## 架构

```
p:\workspace\blog\
├── frontend/          # Vue 3 + TypeScript 前端
└── backend/           # Django REST Framework 后端
```

### 前端架构 (`frontend/`)

**技术栈：**

- Vue 3 (Composition API + `<script setup>`)
- TypeScript 严格模式
- Vite 构建工具
- Tailwind CSS 样式
- Pinia 状态管理
- unplugin-vue-router 文件路由
- vue-i18n 国际化（默认中文，英文支持）
- Iconify (Lucide 图标)

**核心目录：**

- `src/pages/` - 文件路由（`.page.vue` 文件）
- `src/layouts/` - 布局组件（Default、Blog、Reading、Project、Admin）
- `src/components/` - 按功能组织（home、blog、projects、life、search、ui、effects）
- `src/stores/` - Pinia 状态管理（content、theme、search、auth、aiChat、admin）
- `src/composables/` - Vue 组合式函数（useDateFormat、useHeadings、useReadingTime）
- `src/utils/` - 工具函数（contentLoader、markdown、seo、anthropic AI）
- `src/api/` - API 客户端和服务（client.ts、config.ts、types.ts、services.ts）
- `src/types/` - TypeScript 类型定义
- `src/locales/` - i18n 翻译文件（zh-CN.json、en-US.json）
- `src/content/` - Markdown 内容文件（含 frontmatter）
- `config/tailwind.config.js` - 设计系统令牌

**设计系统：**

- 主色调渐变：`#60A5FA` (蓝色) → `#A78BFA` (紫色)
- 浅色主题：背景 `#F8FAFC`，表面 `#FFFFFF`
- 深色主题：背景 `#0B1120`，表面 `#111827`
- 字体：Inter（无衬线）、Fira Code（等宽）
- 动效：弹簧缓动 `cubic-bezier(0.22, 1, 0.36, 1)`
- 阅读宽度：最大 `65ch`

**内容 Frontmatter 结构（Zod 验证）：**

```yaml
title: "字符串"
description: "字符串"
date: "YYYY-MM-DD"
tags: ["标签1", "标签2"]
category: "blog" | "projects" | "life" | "notes"
locale: "zh" | "en"
featured: 布尔值
draft: 布尔值
# 可选字段：
image: "字符串"
readingTime: 数字
# 项目专属：
stars: 数字
forks: 数字
repo: "字符串"
demo: "字符串"
techStack: ["字符串"]
status: "active" | "maintenance" | "archived"
# 生活专属：
coverImage: "字符串"
```

### 后端架构 (`backend/`)

**技术栈：**

- Django 5.2.9 + DRF 3.16.0
- MySQL 8.0 数据库
- Redis 缓存
- Elasticsearch 8.17.2 全文搜索
- Celery 5.4.0 异步任务
- JWT 认证（djangorestframework-simplejwt）

**核心 Django 应用：**

- `users/` - 认证、用户管理（JWT，角色：admin/editor/user）
- `articles/` - 文章、阅读历史、版本控制
- `categories/` - 分类管理（含类型：blog/projects/life/notes）
- `tags/` - 标签系统（含颜色）
- `comments/` - 评论（支持 Giscus 集成、嵌套回复）
- `search/` - Elasticsearch 全文搜索
- `stats/` - 统计分析（热度分数、热门文章缓存）
- `config/` - 设置、URL、Celery 配置

**API 结构：**

- 基础 URL：`http://localhost:8000/api`
- 端点按资源组织：`/auth/`、`/articles/`、`/categories/`、`/tags/`、`/comments/`、`/search/`、`/stats/`
- 统一响应格式：`{ code, message, data }`
- JWT Token 自动刷新在 apiClient 中处理

**后端核心功能：**

- **用户系统**：自定义 User 模型、角色权限（admin/editor/user）、JWT 认证
- **文章系统**：草稿/发布/归档状态、自动生成 slug、阅读时间计算、版本控制、点赞（支持匿名）、相关文章推荐
- **评论系统**：嵌套回复、访客评论、Giscus 集成、状态审核（待审核/已通过/垃圾/已删除）
- **搜索系统**：Elasticsearch 多字段搜索（标题权重 3x）、高亮显示、搜索建议、模糊匹配
- **统计系统**：每日统计、文章热度分数、热门文章缓存、用户行为追踪
- **图片上传**：按分类组织目录、文件验证、UUID 命名

## 开发命令

### 前端 (`cd frontend`)

```bash
# 开发
npm run dev              # 启动 Vite 开发服务器（端口 5173）

# 构建
npm run build            # 类型检查 + 生产构建
npm run type-check       # 仅 TypeScript 类型检查

# 预览
npm run preview          # 本地预览生产构建
```

### 后端 (`cd backend`)

```bash
# 环境配置
cp .env.example .env     # 配置环境变量

# 数据库
python manage.py makemigrations
python manage.py migrate
python manage.py createsuperuser

# 开发服务器
python manage.py runserver

# Celery（需单独终端）
celery -A config worker -l info
celery -A config beat -l info

# 测试
pytest                   # 运行所有测试
pytest --cov=. --cov-report=html  # 生成覆盖率报告

# 代码质量
black .                  # 格式化代码
flake8                   # 代码检查

# Django 工具
python manage.py createsuperuser
python manage.py shell
```

### Docker 部署

```bash
# 从项目根目录
docker-compose up -d     # 启动所有服务
docker-compose logs -f   # 查看日志
docker-compose down      # 停止服务
```

服务列表：backend、celery_worker、celery_beat、mysql、redis、elasticsearch、flower

## 重要架构说明

### 内容加载策略

前端有**两种内容来源**：

1. **Mock 数据** (`src/stores/content.ts`) - 开发用的硬编码内容
2. **Markdown 文件** (`src/content/*.md`) - 通过 Vite 的 `import.meta.glob` 加载的实际内容

`contentLoader.ts` 工具在构建时加载 Markdown 文件。生产环境应使用后端 API。

### 认证流程

- 前端使用存储在 localStorage 中的 JWT Token（`access_token`、`refresh_token`）
- API 客户端 (`src/api/client.ts`) 在 401 错误时自动刷新 Token
- `main.ts` 中的路由守卫保护 `/admin/*` 路由
- 后端使用 SimpleJWT，访问 Token 有效期 60 分钟，刷新 Token 7 天

### 路由

- **文件路由**：在 `src/pages/` 中创建 `[slug].page.vue` 实现动态路由
- **路由块**：在 `.page.vue` 文件中使用 YAML frontmatter 定义路由元信息
- 类型自动生成在 `types/auto-routes.d.ts`

### 状态管理

- **content.ts** - Mock 文章数据（将被 API 调用替换）
- **content.api.ts** - 基于 API 的内容存储（管理操作）
- **auth.api.ts** - 认证存储
- **theme.ts** - 主题持久化（浅色/深色/系统）
- **search.ts** - 搜索模态框状态

### 样式规范

- 使用 Tailwind 工具类
- 深色模式：`dark:` 前缀
- 自定义动画在 `tailwind.config.js` 中定义
- 设计令牌在 `/src/design-tokens/`（如果存在）
- 玻璃态效果使用 `backdrop-blur-glass`

### 类型安全

- 启用严格 TypeScript 模式
- Zod schema 验证 frontmatter
- API 类型与后端 Django 模型对应
- 通过路径别名自动导入类型（`@/types/*`）

## API 客户端使用

```typescript
import { apiClient } from '@/api/client'
import { API_ENDPOINTS } from '@/api/config'

// GET 请求
const data = await apiClient.get<ApiResponse<Article[]>>(API_ENDPOINTS.ARTICLES.LIST)

// POST 请求
const result = await apiClient.post<ApiResponse<AuthResponse>>(
  API_ENDPOINTS.AUTH.LOGIN,
  { username, password }
)
```

## 常见任务

### 添加新博客文章

1. 在 `frontend/src/content/` 中创建 Markdown 文件
2. 添加必需的 frontmatter 字段
3. 运行 `npm run dev` - 内容通过 Vite 自动加载

### 添加新页面

1. 在 `frontend/src/pages/` 中创建 `.page.vue` 文件
2. 如需元信息使用 `<route>` 块
3. 路由自动生成

### 后端 API 修改

1. 修改 `[app]/models.py` 中的模型
2. 更新 `[app]/serializers.py` 中的序列化器
3. 更新 `[app]/views.py` 中的视图
4. 在 `[app]/urls.py` 中注册 URL
5. 运行 `python manage.py makemigrations && python manage.py migrate`

### 管理后台访问

- URL：`http://localhost:8000/admin/`
- 前端管理：`/admin/` 路由（需要认证）
- 凭证：通过 `python manage.py createsuperuser` 创建

## 环境配置

**前端 (`.env`)：**

```bash
VITE_API_BASE_URL=http://localhost:8000/api
```

**后端 (`.env`)：**

```bash
SECRET_KEY=your-secret-key
DEBUG=True
DB_NAME=nano_banana_db
DB_USER=root
DB_PASSWORD=admin
DB_HOST=localhost
DB_PORT=3306
REDIS_HOST=localhost
REDIS_PORT=6379
ELASTICSEARCH_HOST=localhost
ELASTICSEARCH_PORT=9200
```

## 当前开发状态

- ✅ 前端：基础结构和 Mock 数据完成
- ⚠️ 前端：API 集成部分实现（stores 在 `*.api.ts` 文件中）
- ✅ 后端：核心模型和 API 端点已实现
- ⚠️ 后端：搜索（Elasticsearch）需要配置
- ⚠️ 后端：Celery 任务需要实现

在此代码库工作时，应优先完成前后端 API 集成，而非添加新功能。

---

## AI 工作规范

### 📝 错误记录与学习

**原则**：记录重复出现的问题，避免下次再犯错。

当出现以下情况时，必须在本文档末尾的"常见错误与解决方案"章节中记录：

1. **重复犯错的场景**
   - 同样的错误出现 2 次及以上
   - 用户明确指出"这个问题之前说过"
   - 修改后再次引入相同问题

2. **记录格式**
   ```markdown
   ### 错误 N：[简短描述]

   **错误现象**：[具体表现]
   **原因分析**：[为什么会犯这个错误]
   **解决方案**：[正确的做法]
   **首次发现**：YYYY-MM-DD
   ```

3. **检查机制**
   - 开始任务前，先查看"常见错误与解决方案"章节
   - 避免重复记录已存在的问题
   - 定期清理已解决的旧记录（保留 3 个月）

### 🐛 调试代码管理规范

**原则**：调试代码应在问题解决后立即清除，保持代码库整洁。

#### 必须清除的调试代码

调试完成后，必须清除以下类型的调试代码：

1. **console.log 调试语句**
   - 所有 `console.log()`、`console.warn()`、`console.error()`
   - 调试信息输出（如 `🔍`、`✅`、`❌` 等表情符号标记的日志）
   - 变量状态跟踪输出

2. **临时调试变量**
   - 命名为 `debug`、`test`、`temp` 的临时变量
   - 用于调试的计算属性或方法

3. **注释掉的调试代码**
   - 被注释的 watch 监听器
   - 被注释的 console.log 语句

#### 何时清除调试代码

**必须立即清除**：
- ✅ Bug 已修复并验证通过
- ✅ 功能实现完成并测试通过
- ✅ 用户确认问题已解决

**可以暂时保留**（需添加 TODO 注释）：
- ⚠️ 复杂问题的多轮调试中（不超过 24 小时）
- ⚠️ 需要用户反馈验证的问题（添加 `// TODO: 验证后删除`）

#### 调试代码最佳实践

**1. 添加调试代码时**
```typescript
// ❌ 不好：直接添加调试代码
console.log('🔍 筛选器状态:', filterState)

// ✅ 好：添加可追踪的调试代码
console.log('🔍 [LIFE-FILTER] 筛选器状态:', filterState)
// TODO: BUG-修复日期筛选后删除
```

**2. 清除调试代码检查清单**
```markdown
## Bug 修复后检查清单

- [ ] 问题已复现并定位
- [ ] 修复方案已实施
- [ ] 修复已测试验证
- [ ] **所有调试 console.log 已清除**
- [ ] **临时调试变量已删除**
- [ ] **注释掉的调试代码已删除**
- [ ] 代码已提交到版本控制
```

**3. 常见调试位置**
以下文件容易遗留调试代码，需特别注意：

**前端**：
- `src/stores/*.ts` - Pinia 状态管理
- `src/pages/*/*.vue` - 页面组件
- `src/components/**/*.vue` - 可复用组件
- `src/api/*.ts` - API 客户端

**后端**：
- `*/views.py` - Django 视图
- `*/serializers.py` - 序列化器
- `*/models.py` - 模型方法

#### 调试工具推荐

**Vue DevTools**（替代 console.log）：
- 组件状态检查
- Pinia store 监控
- 事件追踪
- 性能分析

**Django Debug Toolbar**（替代 print）：
- SQL 查询分析
- 请求/响应检查
- 性能监控

#### 违反规范的后果

- 遗留调试代码会导致生产环境性能下降
- 暴露敏感信息（如用户数据、API 密钥）
- 影响代码可读性和维护性
- 增加代码审查负担

### 🔄 文档同步规范

**原则**：修改功能后，必须同步更新相关文档。

#### 必须更新的场景

**1. 修改了代码实现**
- [ ] 更新 [SYSTEM_STATUS.md](SYSTEM_STATUS.md) 中的对应状态
- [ ] 如果修改了架构，更新本文档相关章节
- [ ] 如果添加/删除了文件，更新目录结构说明

**2. 修改了 API 接口**
- [ ] 更新本文档的"API 结构"章节
- [ ] 更新 [SYSTEM_STATUS.md](SYSTEM_STATUS.md) 中的 API 状态
- [ ] 如果有 API 文档，同步更新

**3. 修改了配置或环境**
- [ ] 更新本文档的"环境配置"章节
- [ ] 更新 [SYSTEM_STATUS.md](SYSTEM_STATUS.md) 的部署状态

**4. 修复了 Bug**
- [ ] 在 [SYSTEM_STATUS.md](SYSTEM_STATUS.md) 的"已知问题"中更新状态
- [ ] 如果是重复错误，记录到"常见错误与解决方案"

#### 更新检查清单

完成任何代码修改后，必须执行以下检查：

```markdown
## 修改后检查清单

- [ ] 代码已测试并正常运行
- [ ] [SYSTEM_STATUS.md](SYSTEM_STATUS.md) 已更新（状态/进度/版本记录）
- [ ] 如果修改了架构，[CLAUDE.md](CLAUDE.md) 已更新
- [ ] 如果是重复错误，已记录到"常见错误与解决方案"
- [ ] 版本历史已添加记录（如果涉及重要功能）
```

#### 更新优先级

| 修改类型 | 必须更新 | 建议更新 |
|----------|----------|----------|
| 修复 Bug | SYSTEM_STATUS.md | - |
| 新增功能 | SYSTEM_STATUS.md + CLAUDE.md | - |
| 修改 API | SYSTEM_STATUS.md + CLAUDE.md | API 文档 |
| 架构调整 | SYSTEM_STATUS.md + CLAUDE.md | - |
| 性能优化 | SYSTEM_STATUS.md | - |
| 配置变更 | SYSTEM_STATUS.md + CLAUDE.md | 部署文档 |

#### 文档更新示例

```markdown
### 示例：完成 Elasticsearch 搜索功能

1. 代码修改：
   - backend/search/views.py: 实现搜索视图
   - frontend/components/SearchModal.vue: 集成搜索 API

2. 文档更新：
   - SYSTEM_STATUS.md:
     * 搜索功能完成度：40% → ✅
     * 整体完成度更新
     * 版本历史添加记录
     * last_updated 更新

3. 验证：
   ✅ 代码测试通过
   ✅ SYSTEM_STATUS.md 已更新
   ✅ 状态标记正确
```

---

## 常见错误与解决方案

> 本章节记录重复出现的问题，避免重复犯错。

*（暂无记录）*
